FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source code
COPY . .
COPY vite.config.ts ./

# Build
RUN npm run build

# Expose port
EXPOSE 3000

# Start dev server (for production, use nginx)
CMD ["npm", "run", "dev", "--", "--host"]
